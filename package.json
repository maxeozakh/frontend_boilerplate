{
  "name": "frontend_boilerplate",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "test",
    "start": "esbuild app.tsx --bundle --watch --outfile=build/out.js",
    "build": "esbuild app.tsx --bundle --minify --outfile=build/out.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "esbuild": "^0.14.13",
    "react": "^17.0.2",
    "react-dom": "^17.0.2"
  },
  "devDependencies": {
    "typescript": "^4.5.5"
  }
}
